<h1>Welcome</h1>

<% @users.each do |user| %>
  <% if current_user.friends.include?(user) %>
    <%= user.name %> | FRIENDS | <%=link_to "Remove Friend", remove_path(user), :data => {:confirm => "Are you sure you want to remove #{user.name} as your friend?"}%> <br>
  <% elsif current_user.requests_to.include?(user) %>
    <%= user.name %> | Request Pending <br>
  <% elsif current_user.requests_from.include?(user)%>
    <%= user.name %> | <%= link_to "Accept", accept_path(user) %> <br>
  <% else %>
    <%= user.name %> |
    <%= link_to "Add Friend", request_path(user) %><br>
  <% end %>
<% end %>
