<h1>Editing Listing</h1>

<%= render 'form', listing: @listing %>

<td><%= link_to 'Back', listing_path %></td> |
<td><%= link_to 'Edit Your Apartment', edit_listing_path(@listing) %></td> |
<td><%= link_to 'Delete Your Apartment', @listing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
